/* Minimal stub for the Zephyr MQTT client API.
 *
 * Only the symbols referenced by the T16 skeleton are stubbed:
 *   - `struct mqtt_client` (forward declaration, no fields used in skeleton)
 *
 * Real definitions live in upstream Zephyr; this stub exists only so the
 * C syntax gate (`cpp -fsyntax-only`) can validate generated main.c files
 * without a full Zephyr checkout.
 */
#ifndef DEMOL_STUB_ZEPHYR_NET_MQTT_H
#define DEMOL_STUB_ZEPHYR_NET_MQTT_H

struct mqtt_client {
    int _placeholder;
};

#endif /* DEMOL_STUB_ZEPHYR_NET_MQTT_H */
