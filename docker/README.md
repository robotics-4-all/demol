To start the API and App containers run:

```bash
docker compose -f docker/docker-compose.yml up --remove-orphans
```

To stop the containers run:

```bash
docker compose -f docker/docker-compose.yml down
```
