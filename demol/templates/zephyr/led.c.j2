/*
 * Zephyr LED driver (GPIO output).
 * Generated by demol — uses Zephyr's gpio_dt_spec + gpio_pin_*_dt helpers.
 */

#include <zephyr/device.h>
#include <zephyr/drivers/gpio.h>

#include "{{ name }}.h"

static const struct gpio_dt_spec led = GPIO_DT_SPEC_GET(DT_INST(0, gpio_leds), gpios);

int led_init(void)
{
    if (!gpio_is_ready_dt(&led)) {
        return -ENODEV;
    }
    return gpio_pin_configure_dt(&led, GPIO_OUTPUT);
}

int led_set(bool on)
{
    return gpio_pin_set_dt(&led, on ? 1 : 0);
}
