from ADCDifferentialPi import ADCDifferentialPi as ADCimported
import time
from .peripheral import Sensor

#Get the value from the device, else leave it as it is.
PGA = {{gain | default(1)}}
BIT_MODE = {{resolution | default(18)}}
CHANNEL = {{channel | default(1)}} #Same as above
PRIMARY_SLAVE_ADDRESS = 0x{{slaveAddr | default(68)}}
SECONDARY_SLAVE_ADDRESS = 0x{{secondary_slave_address | default(69)}}
MAX_FREQUENCY = {{max_frequency | default(240)}}

class {{ driver_class_name }}(Sensor):
    def __init__(self):
        super().__init__(
            name="{{ instance }}", 
            connections={{ conn }}, 
            attributes={{ attributes }}
        )
        self.adc = None
        self.max_frequency = MAX_FREQUENCY
    
    def initialize(self):
        self.adc = ADCimported(PRIMARY_SLAVE_ADDRESS, SECONDARY_SLAVE_ADDRESS)
        self.adc.set_pga(PGA)
        self.adc.set_bit_mode(BIT_MODE)
        super().initialize()

    def read(self):
        voltage = self.adc.read_voltage(channel=CHANNEL)
        data = {"voltage": voltage}
        self.update_state_data(data)
        return data

    def get_max_frequency(self):
        return self.max_frequency

    def disconnect(self):
        super().disconnect()

